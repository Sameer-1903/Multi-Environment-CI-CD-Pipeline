apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector: { app: backend }
  ports:
  - port: {{ .Values.backend.service.port }}
    targetPort: 8080
    protocol: TCP
  type: {{ .Values.backend.service.type }}
